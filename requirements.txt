# =====================================================
# Web Server & API Framework (The Gateway)
# =====================================================
fastapi==0.110.0
uvicorn==0.27.1
python-multipart==0.0.9
pydantic==2.6.3

# =====================================================
# AI & Image Processing (The "Voice" Layer)
# Used ONLY for descriptive readings & explanations.
# Match % and destiny are frontend-seeded & deterministic.
# =====================================================
google-generativeai==0.4.1
Pillow==10.2.0

# =====================================================
# Database & Environment (The Memory)
# =====================================================
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# =====================================================
# Async, Compatibility & Math (The Engine)
# Required for FastAPI internals, deterministic scaling,
# and symmetric pair-unit calculations
# =====================================================
anyio==4.3.0
typing-extensions==4.10.0
numpy==1.26.4

# =====================================================
# Chat, Moderation & Realtime Stability
# (Pinned to avoid production drift)
# =====================================================
starlette==0.36.3

# =====================================================
# Media Storage (Profile Photos & Moderation Review)
# =====================================================
cloudinary==1.39.0

# =====================================================
# Admin Moderation, Scoring & Background Tasks
# =====================================================
apscheduler==3.10.4
