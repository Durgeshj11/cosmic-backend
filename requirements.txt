# =====================================================
# Web Server & API Framework (The Gateway)
# =====================================================
fastapi==0.110.0
uvicorn==0.27.1
# Required for handling multiple photo files and form data
python-multipart==0.0.9
pydantic==2.6.3
# Required for WebSocket support in Starlette/FastAPI
websockets==12.0

# =====================================================
# Real-Time & Scaling Engine (The "Glue")
# Handles the Symmetric Pair-Unit Pub/Sub logic via Redis
# =====================================================
redis==5.0.3
aioredis==2.0.1
# High-speed parser for Redis data
hiredis==2.3.2

# =====================================================
# Symmetric Notification Engine
# Required for sending Firebase Push Notifications
# =====================================================
firebase-admin==6.5.0

# =====================================================
# AI & Image Processing (The "Voice" Layer)
# Used for the "Nuclear" Privacy Shield & Acoustic Moderation
# =====================================================
google-generativeai==0.4.1
# NEW: Required for Whisper Acoustic Moderation (Aesthetic framing: Resonance Stability)
openai==1.50.0
Pillow==10.2.0

# =====================================================
# Database & Environment (The Memory)
# Handles Cosmic Profiles, Matches, and Secure Messages.
# =====================================================
sqlalchemy==2.0.28
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# =====================================================
# Async, Compatibility & Math (The Engine)
# Supports deterministic seeding for symmetric match scores.
# =====================================================
anyio==4.3.0
# UPDATED: Raised to 4.12.2 to resolve conflict with OpenAI 1.50.0
typing-extensions==4.12.2
numpy==1.26.4

# =====================================================
# Chat, Moderation & Realtime Stability
# Core for the AI-moderated chat logic.
# =====================================================
starlette==0.36.3

# =====================================================
# Media Storage (Profile Photo Gallery & Audio Vibrations)
# Stores and serves multiple images and voice vibrations.
# =====================================================
cloudinary==1.39.0

# =====================================================
# Background Tasks & Scheduling
# =====================================================
apscheduler==3.10.4